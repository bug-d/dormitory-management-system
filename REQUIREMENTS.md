# 需求

## 用户目标

- 管理员可以维护用户、学生、宿舍并处理申请。
- 宿舍管理员只能查看和管理获授权的宿舍及相关申请。
- 学生可以查看可用宿舍、提交入住/调宿申请并查看进度。

## 功能需求

- JWT 登录与基于角色的接口授权。
- 用户、学生、宿舍的查询与管理。
- 入住、调宿、退宿申请与审核。
- 管理员、宿管、学生适配的数据看板。

## 非功能需求

- 不记录明文密码、Token 或数据库密码。
- 公开仓库不得包含秘密或生成目录。
- 前后端必须能在干净环境完成生产构建。

## 约束

- 后端要求 JDK 17、Maven 3.9+、MySQL 8。
- 前端要求 Node.js 与 npm。

## 验收标准

- `mvn test` 与 `npm run build` 成功。
- 宿管控制器不再为空，前端核心页面不再依赖硬编码模拟数据。
- 配置使用环境变量，并提供可复制的示例值。
- 项目文档包含本地启动与数据库初始化说明。
